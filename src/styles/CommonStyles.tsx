import { StyleSheet } from "react-native";

const TextStyle = StyleSheet.create({
	center: {
		flex: 1,
		backgroundColor: "#fff",
		alignItems: "center",
		justifyContent: "center",
	},
});

export { TextStyle };
